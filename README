Edit Zosi firmware Firmware for NVR 

1. download image to image folder
2. create folder with name of image
2. use fdisk -u -l image/<image name> | tee <image name>.txt to determine type of image
    and dump to txt
3. 
**3. use dd to extract image to files and folders
